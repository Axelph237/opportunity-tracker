# Locked runtime dependencies.
#
# Every version is pinned, transitive ones included, so an install six months
# from now resolves to the same tree this was tested against. Plain pip reads
# this; no extra tooling required.
#
# To add or upgrade a dependency: edit the "declared" list below, install it,
# then regenerate the lock with
#     .venv/bin/pip freeze | sort > /tmp/freeze.txt
# and reconcile. Build-only packages (e.g. fonttools) must not end up here.
#
# Declared: fastapi, uvicorn[standard], httpx, beautifulsoup4, pypdf,
#           apscheduler, python-dotenv, python-multipart

APScheduler==3.11.3
PyYAML==6.0.3
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
beautifulsoup4==4.15.0
certifi==2026.7.22
click==8.5.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.20
pydantic==2.13.5
pydantic_core==2.46.5
pypdf==6.19.0
python-dotenv==1.2.3
python-multipart==0.0.32
soupsieve==2.9.2
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
tzlocal==5.4.4
uvicorn==0.53.0
uvloop==0.22.1
watchfiles==1.3.0
websockets==17.1
