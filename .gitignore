__pycache__/
*.py[cod]
.venv/
venv/
data/
*.db
*.db-wal
*.db-shm
.env
node_modules/
dist/
# The LaTeX engine scripts/install.sh downloads when the machine has none.
vendor/
.DS_Store
logs/
resume.pdf
resume.txt
resume.tex
# The render the app publishes for scoring, rebuilt from the LaTeX source.
resume-active.pdf
resume-active.txt
prompt.md

# Satoshi is fetched by scripts/install.sh, not redistributed. The ITF Free
# Font License permits self-hosting but not making the font available to third
# parties, which publishing it here would do.
frontend/public/fonts/*.woff2
